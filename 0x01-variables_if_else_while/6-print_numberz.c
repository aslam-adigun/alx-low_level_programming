#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'Print letters of the alphabet'
 *
 * Return: Always 0 (Success)
 */

int main(void)

{
	int m;

	for (m = 0; m < 10; m++)
	{
		putchar(m);
	}

	putchar('\n');
	return (0);
}
